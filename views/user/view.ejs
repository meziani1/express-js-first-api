<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <!-- Bootstrap CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet"> 
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h3>User Profile : <%= arr._id %></h3>
            </div>
            <div class="card-body">
                <div class="row">
                   
                    <div class="col-md-8">
                        <h4><%= arr.usernameee %></h4>
                        <p><strong>Age:</strong> <%= arr.age %></p>
                        <!-- <p><strong>Phone:</strong> <%= arr.phone %></p> -->
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS and dependencies -->
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
